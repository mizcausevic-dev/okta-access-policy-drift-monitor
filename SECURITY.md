# Security

Do not commit real Okta exports, tenant IDs, user IDs, group names, app IDs, policy IDs, sign-in logs, customer identifiers, tokens, credentials, screenshots, or audit evidence from production systems.

Use synthetic policy telemetry only for public portfolio proof.
